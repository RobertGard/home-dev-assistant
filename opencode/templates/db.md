---
description: Database tools — explore schema, analyze queries, generate seeds, review migrations
agent: db-analyst
subtask: true
---

Use the database-tools skill:
1. EXPLORE: list tables, sizes, row counts, relations
2. SCHEMA: inspect specific table structure, indexes, constraints
3. QUERIES: run EXPLAIN on slow queries, find missing indexes
4. MIGRATIONS: review pending migrations for safety
5. SEED: generate realistic seed data if requested

Report: schema overview, performance findings, migration safety, actionable recommendations.
